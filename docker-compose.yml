version: '3.8'

services:
  asset-manager:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: asset-manager
    ports:
      - "5000:5000"
    volumes:
      - asset-data:/app/data
    restart: unless-stopped

volumes:
  asset-data:
